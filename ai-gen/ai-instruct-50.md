# AI instruction 50

Image are still not rendered in the email template:

```html
<td style="padding:24px;text-align:center;background-color:#ffffff;border-bottom:1px solid #e2e8f0">
    <img alt="CWClock" height="36" style="height:36px">
</td>
```

I want also the button to be centered (forgotten password, confirmation's emails).

I'd like the `logoDataURI` to also work with jpeg, etc (I just have the b64 string in database).
