# AI instruction 69

## Git external connection

I want also to specify a path in the git external connection. If it's empty, create the file and dir at the root of the repo or inside the path otherwise.

## MFA

A user can have multiple MFA methods set, one of them is sufficient to authenticate when there's multiple methods set.

If the superuser disable the MFA for a user, it remove all the methods.
