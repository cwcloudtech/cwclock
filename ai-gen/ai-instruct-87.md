# AI instruction 87

## Cli

The cli is [here](../cwclock-cli)

This cli is forked from `qwctl`, I want you to rename `qwctl` to `cwclock` everywhere.

Also remove the following commands and useless code associate:

```shell
go run main.go search
go run main.go alertmanager
go run main.go create
go run main.go delete
go run main.go infos
go run main.go ingest
go run main.go list
```
