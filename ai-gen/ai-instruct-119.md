# AI instruction 119

## Mobile app

**1/** */ The QR code doesn't scan the config QR it stay on camera without recognition. I want you to use the same component as cwc-chat: `https://gitlab.cwcloud.tech/oss/cwcloud/cwc-chat.git` (it's already clone here: `~/cwc-chat-mobile`).

Not a plain screen scanner.

**2/** */ I always a return/go back button on each screen. For example when I choose "manually config" I want to be able to choose back to scan the QR code.

**3/** */ In manual config, do not propose a plain config textarea, keep only the form with each separate fields (api url pre-filled with `https://api.cwclock.me`, api key, etc).

**4/** I also want lighmode and darkmode and be able to switch language in the preference settings once I'm connected.
