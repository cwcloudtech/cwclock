# AI instruction 17

Move the import button on the right of start button (for admin and owner).

In both time record edit form or detailed report edit entry the selected user should be set with the right one.

User is not printed in the detailed report webview : I want the First Name and Last name.
Same in the PDF tables exports

The column should not be truncated but do `\n` if it's to big in the table reports.
