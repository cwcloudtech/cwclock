# AI instruction 72

## Version

For the frontend, instead of calling `/v1/manifest` to get the version, I want to read directly the `manifest.json` file which is supposed to be on the same root directory (check the `Dockerfile`).
