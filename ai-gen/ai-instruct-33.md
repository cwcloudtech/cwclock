# AI instruction 33

To complete previous [ai-instruct-32](./ai-instruct-32.md), even disabled users should be counted in report or invoices (we shouldn't take into account the global role of user).

Also I'd like to redesign the time record webview `project` and `user` autocomplete by autocomplete dropdown select like it's done in the invoice screen.
