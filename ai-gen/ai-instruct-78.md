# AI instruction 78

## Export format

I want you to completely replace the PDF summary and detailed export by the summary and detailed portrait A4.

In the list of possible export I only want to see "summary" and "export" as csv and pdf.

## Flatten directory

I want this checkbox to appear only in the organization external's connection but not in the export job's target.

If you share the same form as webcomponent add a boolean flag to disable this input.

And if you share the same backend function, it has to be flat for export job's target in any case, and not flat for the organization's external connection by default.

No need for data migration and smoke tests.
