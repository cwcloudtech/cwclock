# AI instruction 4

## Profile

Edit profile must also handle password modification (with a confirmed field).
If the value remain empty do not update the password.

# User admin

List of user should aligned every items (Denomination, email, role, edit button).
Admin should also be able to update the email.

Change the edit button by the same icon used in time record screen and add a delete button with confirmation modal exactly the same way.

Also add filters to only see disabled|confirmed|superuser|all

# Organization admin

`superuser` must be able to edit or delete an organization from other user with an admin screen like for managing the users.

# Profile navbar dropdown

Replace "logout" button by an icon for disconnecting and a tooltip (using same design of other tooltips).

# Stay Signed In

Check if the checkbox is used by the backend.

If it's not the case, remove it.
