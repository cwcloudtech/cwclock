# AI instruction 127

## Mobile

* On the report screen, I want also the client/project AND member filters just below the start date/enddate fields (same design, two filters in one line).
* The share button is not working on pdf document, I want it to propose other applications like drive, whatsapp, etc.
* I want also in the settings an upgrade button synchronizing with the endpoint `https://api.cwclock.me/v1/manifest` (`version` field), if it's superior propose an upgrade button (also with an icon + label) downloading and replacing the apk with `https://www.cwclock.me/cwclock-v{version}.apk`. Add a download progression bar (also I want you to keep the same configuration, meaning api url, api key and orga id).
