# AI instruction 42

## External connection

Add a swicth on/off to expand the invoices in `YYYY/MM.{MonthName}` (the current behavior) or to keep the files in a flat directory (for some accounting software listening google drive or S3 it's required to avoid subfolders).
