# AI instruction 41

## Security

`GET /organizations` shouldn't display the following fields:

* access key/secret key of S3 external connections
* google service account for drive external connections

It's security issue.
