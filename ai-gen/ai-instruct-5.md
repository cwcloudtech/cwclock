# AI instruction 5

## UX bugs to fix

The logout tooltip is always displayed even when I'm not hovering the logout icon in the navbar.

## Time record autocomplete

Transform the project select by a dropdown-autocomplete that is also showing the `customer - {project name}`

The automplete has to also filter by customer name first then project name.

## Organization admin

I want to see the email of the owner in the list and also an icon allowing the superuser to transfer ownership (using a popin with an autocomplete for the new owner).

The icon in the sidebar should match the organization with a building icon adding a shield like the user icon. It's important to stay consistant in the UI.
