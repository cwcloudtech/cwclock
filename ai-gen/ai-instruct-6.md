# AI instruction 6

## Dark/light mode

Add dark/light mode (set by default on the operating system configuration) which can be switch in the connected profile dropdown with a changing icon and tooltip.

## Toast of success

Add a toast when an API call is success also (like updating profile, etc.) except time record creation (but update or delete yes).
