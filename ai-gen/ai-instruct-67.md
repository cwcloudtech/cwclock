# AI instruction 66

## Organization banking details

Add the optional IBAN and BIC fields in the organization payload.

If both are set, add it into the invoice PDF as well after contact in a single entry "IBAN / BIC".

If there's only the IBAN set, display "IBAN" with the IBAN only in the invoice.
