# AI instruction 79

## Export job

I want also the invoices PDF in the choices of export.

In this case the system has to send the existing invoices during the period for the selected clients (ignore projects selected only for invoices). Do not generate new invoices.

The remaining time has to be computed by the API and returned to the frontend and be exact.
