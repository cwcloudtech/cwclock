# AI instruction 125

## Mobile app

* In the report and invoice generation screen, put the fields _start date_ and _end date_ on the same line centered.
* In the `+` button on time record for _Add all-day record_ add a switch to choose _half day_ instead off _all day_ on the same line of _Day_ input
* In the invoice add a button icon to be able to change the status
