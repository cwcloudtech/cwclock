# AI instruction 23

## Swagger/OAS

Add the ability to authenticate (unprotect) with the `X-Api-Key` header alongside `Authorization` header.

## Copy uuid

On each management screen (organization, client, user, project) I want a copy id button (icon) to copy the uuid in the clipboard.
