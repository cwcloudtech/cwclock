# AI instruction 18

## UX/UI

In the timerecord webview, truncate the label/description of the task if it's to big and add `...` and add a tooltip which display the complete label.

The size must be an environment variable `REACT_APP_TASK_LABEL_MAX_LENGTH`.

## PDF export

I want the first line of tables to be bold with a background color `#1cb9f7`.
