{{- if $.Values.api.ingress.enabled -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cwclock-api
  annotations:
    ingress.kubernetes.io/ssl-redirect: {{ $.Values.api.ingress.ssl_redirect | quote }}
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cwclock-api
            port:
              number: {{ $.Values.api.port }}
{{- end -}}
