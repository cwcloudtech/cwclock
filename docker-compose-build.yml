services:

  api:
    image: ${CI_REGISTRY}/cwclock-api:${VERSION}
    build:
      context: .
      dockerfile: Dockerfile
      target: api

  ui:
    image: ${CI_REGISTRY}/cwclock-ui:${VERSION}
    build:
      context: .
      dockerfile: Dockerfile
      target: ui
  ui-and-mobile:
    image: ${CI_REGISTRY}/cwclock-ui:${VERSION}-mobile
    build:
      context: .
      dockerfile: Dockerfile
      target: ui-and-mobile
